class CBA_Extended_EventHandlers;
class CfgVehicles {
    class vtx_MH60M_DAP {
        class USAF_ServiceMenu {
                enabled = 1;
                loadOutCustomization = 0; // Allow user to modify each pylon. Default: 0
                openBays = 0; // Open the bays when entering the menu. Default: 1
                addBaysToggle = 0; // Add a button for the user to toggle the bays. Default: 1
                cameraPos[] = {{0,0,-0.631},326.326,5.972,26.005};
        };
    };

    class vtx_MH60M_DAP_MLASS {
        class USAF_ServiceMenu {
                enabled = 1;
                loadOutCustomization = 0; // Allow user to modify each pylon. Default: 0
                openBays = 0; // Open the bays when entering the menu. Default: 1
                addBaysToggle = 0; // Add a button for the user to toggle the bays. Default: 1
                cameraPos[] = {{0,0,-0.631},326.326,5.972,26.005};
        };
    };
};
